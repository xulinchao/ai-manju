# 本地漫剧工作流架构

最终交付成片，当前优化阶段聚焦可审核、可返工、可复用的图片。

通用引擎位于 _engine，故事各自独立。001、002 为原有测试项目，保持不动。003-一致性验证用于新流程验收。

已实现：画风分层、候选版本与审核登记、多图编辑、遮罩合成、裁切、独立换脸、参考依赖核对、审核看板和分阶段检查。实际运行方式统一见 [_engine/图片制作流程.md](_engine/图片制作流程.md)。

生产顺序：画风初稿 → 用户审核 → 角色/场景/道具主参考 → 用户审核 → 角度与机位派生 → 用户审核 → 分镜对照 → 用户审核 → 视频验证 → 声音、字幕、剪辑与成片。

当前 H3 R2V 工作流属于多参考图条件，旧 firstlast 命名不能代表结束帧绑定。视频接口已加语义提示和图片审核门；本轮不投产视频。

本轮依赖现有 Z-Image 与 Qwen Image Edit 2509，无新增模型。参考图只能帮助生成；固定区域靠局部合成保护，身份和空间可读性由人工审核。
